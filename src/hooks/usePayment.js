export function usePayment() {
  // TODO: Stripe integration
  // const stripe = useRef(null)
  // useEffect(() => {
  //   stripe.current = Stripe(process.env.VITE_STRIPE_PUBLIC_KEY)
  // }, [])
  //
  // async function checkout(priceId) {
  //   await stripe.current.redirectToCheckout({ lineItems: [{ price: priceId, quantity: 1 }] })
  // }

  return {}
}
